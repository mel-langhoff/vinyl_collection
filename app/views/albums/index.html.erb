<h1>Vinyls</h1>

<ul>
  <% @albums.each do |album| %>
    <li>
      <%= album.title %> by <%= album.artist.name %>
    </li>
  <% end %>
</ul>

<h2>Search by Album:</h2>
<p><%= form_with(url: search_albums_path, method: "get") do |form| %>
  <%= form.text_field :search, placeholder: "Enter an album name" %>
  <%= form.submit "Search" %>
<% end %>