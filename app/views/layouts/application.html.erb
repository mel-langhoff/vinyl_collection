<!DOCTYPE html>
<html>
  <head>
    <title>VinylCollection</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <section>
      <p><%= link_to "Album List", "/albums" %></p>
      <p><%= link_to "Artist List", "/artists" %></p>
    </section>
    <%= yield %>
  </body>
</html>
